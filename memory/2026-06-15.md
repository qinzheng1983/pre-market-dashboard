

## 2026-06-15 10:21 — GitHub Pages部署根因确认与修复

**最终根因**：GitHub Pages 从 `main` 分支的 `/docs` 目录部署，不是根目录。

- 根目录 `index.html` = 18789 bytes（新，已更新）
- `docs/index.html` = 10059 bytes（旧，Pages实际读取）
- 根目录 `dashboard.html` = 4026 bytes（新，已更新）
- `docs/dashboard.html` = 2808 bytes（旧，Pages实际读取）

**修复**：
- 更新 `docs/index.html` → 18789 bytes（今天的报告）
- 更新 `docs/dashboard.html` → 2851 bytes（日期改为6月15日，描述更新）
- 等待 Pages 构建完成后（约60秒），CDN已刷新

**验证**：
- curl 确认 dashboard.html 显示 "2026年6月15日" + "更新于 2026-06-15 10:21"
- curl 确认 index.html 标题为 "盘前简报 — 2026年6月15日（周一）"

**教训**：
- 部署脚本 `deploy_to_github.py` 必须更新 `docs/` 目录而非根目录
- 需要修改脚本逻辑：读取 Pages 设置中的 `source.path` 字段，确定实际部署目录
- 当前 `source.path` = "/docs"，所以所有文件必须上传到 `docs/` 路径

**部署脚本修复方向**：
1. 先通过 GitHub API 获取 Pages 设置，确认 `source.path`
2. 根据 `source.path` 确定上传路径（根目录或 `docs/`）
3. 统一上传到正确的路径
